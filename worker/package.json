{
  "name": "family-scanner-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for family content scanning using Claude API",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
